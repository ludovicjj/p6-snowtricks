<nav class="navbar navbar-expand-lg navbar-dark bg_custom">
    <a class="navbar-brand" href="{{ path('home') }}">Snow<span class="custom_orange">tricks</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item">
                <a class="nav-link {% if app.request.attributes.get('_route') == 'add_trick' %}active{% endif %}" href="{{ path('add_trick') }}">
                    Ajouter
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link {% if app.request.attributes.get('_route') == 'security_registration' %}active{% endif %}" href="{{ path('security_registration') }}">
                    Inscription
                </a>
            </li>
        </ul>

        <a href="{{ path('security_login') }}" class="btn btn-outline-orange">Connexion</a>
    </div>
</nav>